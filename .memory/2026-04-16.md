## 21:45 [MCP-4] 审核解析功能并修复 7 个 bug + 创建 11 个测试
- 完成：修复 7 个问题（编译错误 3 个 + 逻辑 bug 4 个），创建完整集成测试（11 cases 全通过），增强 pid_inspect 输出完整报告与 JSON。
- 决策：XML 解析器嵌套标签跳过 bug 是 Drawing/General Meta 全空的根因，优先修复；symbol path 乱码前缀通过 UNC 路径提取清理。
- 待办：Summary parser 仍为 no-op（OLE property set 解码待后续实现）；dynamic_attributes 的 class_names 有重复可去重；GUID 提取未实现。

## 23:08 [MCP-1] Phase 2 三项待办全部完成
- 完成：实现 OLE Summary 解析（SummaryInformation + DocumentSummaryInformation 流解码，输出 AppName/Title/Author/CreateTime 等）；GUID 扫描（文本+二进制 16 字节双模式，706 GUIDs from test file）；strings/class_names 去重（HashSet 消除重复）；inspect 报告更新。
- 决策：Summary 解码自行实现 OLE Property Set 二进制解析（VT_LPSTR/VT_LPWSTR/VT_FILETIME/VT_I4），避免引入额外依赖；GUID 扫描同时支持文本和 raw 二进制格式，raw 模式通过 version nibble 过滤误报。
- 待办：uuid/time crate 在 Cargo.toml 中声明但源码未使用可清理；ARCHITECTURE.md 和 README.md 可同步更新。

## 23:50 [MCP-1] Phase 3 二进制记录切分完成
- 完成：实现公共 cluster header 解析器（magic 0x6C90F544 + type/count/body 字段）；PSMcluster0 string table 解析（反向定位 entry1，成功提取 SiteObjects/PreferenceSet/Sheets）；Unclustered Dynamic Attributes 记录解码器（231 条记录、10 个唯一 class、1120+ 个属性字段），含 P&IDAttributes/MultiLinearPattern/SymbolAttr 等；模型层新增 ClusterHeader/IndexedString/AttributeRecord/AttributeField/AttributeValue。
- 决策：DA 记录解码采用 0x0089 type marker 作为记录边界，heuristic 扫描 class name + attribute name/value；PSMcluster0 string table 通过 entry2(index=2) 反推 entry1 位置，避免 header 区的 false match。
- 待办：DA 记录中部分值有 1 字节前缀偏移（如 "2DWG-0201GP06-01.pid"），可进一步微调解码精度；StyleCluster GUID 表解析可作为下一阶段目标。
