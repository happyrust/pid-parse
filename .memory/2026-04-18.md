## 22:00 [MCP-3] Phase 6c 跨引用对象图 + v0.3.0-rc2
- 完成：新增 `src/crossref.rs` 纯派生模块，`CrossReferenceGraph` 含四个子视图（`ClusterCoverage` / `SymbolUsage` / `AttributeClassSummary` / `RootPresence`）；pipeline 末尾接入；report.rs 加 `--- Cross Reference ---` 段；CLI 加 `--crossref` 专项；CHANGELOG/README/ARCHITECTURE 同步。
- 集成决策：rebase 到并行会话推送的 v0.3.0-rc1（Phase 6 关系端点解码）之上，版本升为 **0.3.0-rc2**。rc1 做底层关系边（source/target via Sheet 端点对间接引用），rc2 做上层统计视图与数据完整性检查，两者互补。
- 合并冲突：Cargo.toml / Cargo.lock / CHANGELOG / pid_inspect.rs / model.rs 均有冲突，手动解。注意 rc1 扩展了 `AttributeField.raw_value` / `SheetStream.endpoint_records` / `DynamicAttributesBlob.{record_trailers,relationship_probes}`，crossref 的单元测试 struct 构造器全部补齐。
- lib 测试 **53 通过**（rc1 + rc2 合集），release 构建通过。推送 commit `00a6b94` 到 origin/main。

## 22:45 [MCP-3] Phase 7a Mermaid 可视化 + v0.3.0
- 完成：新增 `src/inspect/mermaid.rs` 纯派生模块，两个导出函数 `object_graph_mermaid(doc)` / `crossref_mermaid(doc)`（及带 options 版本）。对象图用 `graph LR` 渲染 objects + relationships，按 item_type 着色、DrawingID 截短、off-drawing 端点自动占位、默认过滤空 guid 模板关系；交叉引用图用 `graph TB` 四个 subgraph（Cluster Coverage / Symbol Usage / Attribute Classes / PSMroots→CFB Tree）。CLI 加 `--graph-mermaid` 和 `--crossref-mermaid`。
- 决策：不引入 schemars / 任何 mermaid-gen crate，纯字符串输出保持依赖精简；容量控制（max_nodes=200 / max_edges=500 / max_symbols=20 / max_jsites_per_symbol=6）确保大图仍可解析；sanitize 规范化 ID，escape_mermaid 处理 `|` `"` `\` 避免语法冲突。
- 版本：0.3.0-rc2 → **0.3.0**（正式版）——关系边（rc1）+ 跨引用统计（rc2）+ 可视化（7a）三件事闭环交付。
- 测试：`inspect::mermaid` 8 个单元测试（空文档/节点&边/off-drawing/模板过滤/四 subgraph/sanitize/escape/溢出），总 lib 测试 53→62，release 构建通过。
- 待办：Phase 7 剩余 DocVersion2 结构化 / PSMcluster0 完整记录 / StyleCluster 解码 / JSON Schema / 往返序列化。
