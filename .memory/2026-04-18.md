## 22:00 [MCP-3] Phase 6c 跨引用对象图 + v0.3.0-rc2
- 完成：新增 `src/crossref.rs` 纯派生模块，`CrossReferenceGraph` 含四个子视图（`ClusterCoverage` PSM 声明 vs 实际 cluster/sheet 对齐 / `SymbolUsage` symbol_path→JSite 反向索引 / `AttributeClassSummary` 每类记录数+属性名+涉及图号模型号 / `RootPresence` PSMroots 与 CFB 顶层目录对齐标记 STORAGE/STREAM/MISSING）；pipeline 末尾接入；report.rs 加 `--- Cross Reference ---` 段；CLI 加 `--crossref` 专项；CHANGELOG/README/ARCHITECTURE 同步。
- 集成决策：rebase 到并行会话推送的 v0.3.0-rc1（Phase 6 关系端点解码）之上，版本升为 **0.3.0-rc2**；rc1 做底层关系边 (`source_drawing_id`/`target_drawing_id` via Sheet 端点对记录间接引用)，rc2 做上层统计视图与数据完整性检查，两者互补。
- 合并冲突：Cargo.toml / Cargo.lock / CHANGELOG / pid_inspect.rs / model.rs 均有冲突，手动解。model.rs 需注意：rc1 扩展了 `AttributeField.raw_value` / `SheetStream.endpoint_records` / `DynamicAttributesBlob.{record_trailers,relationship_probes}`，crossref 的单元测试 struct 构造器全部补齐。
- lib 测试 **53 通过**（rc1 + rc2 合集），release 构建通过。
- 推送：等合并完成后 push origin main。
- 待办：`DocVersion2` 48B 结构化解码 / Sheet 其他图元（除端点对外）/ PSMcluster0 完整记录 / 往返序列化。
