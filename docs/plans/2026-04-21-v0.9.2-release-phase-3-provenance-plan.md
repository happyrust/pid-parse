# v0.9.2 Release — Phase 3 Provenance Series Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to execute this plan task-by-task.

**Goal:** 把 Phase 3 Step 1-4 整个 provenance series 打包成 **v0.9.2** 快照，写入 CHANGELOG，bump Cargo.toml，并留好待用户手动 `git tag v0.9.2` 的前置条件。

**Architecture:** 这是纯"卫生 pass"，不加功能、不动既有代码，只更新元数据 + 文档。与 Phase 2 → v0.9.1 的 release 节奏一致（参见 commit `a0e8f5d docs(release): v0.9.1 — Phase 2 structural decoding upgrades + CHANGELOG`）。

**Tech Stack:** 纯文档 + Cargo.toml。

---

### Task 1: 版本号与锁

**Files:**
- Modify: `Cargo.toml`
- Modify: `Cargo.lock`（自动同步）

**Step 1: bump Cargo.toml**

把 `version = "0.9.1"` 改为 `version = "0.9.2"`。

**Step 2: 同步 Cargo.lock**

Run: `cargo build --quiet`

Expected: `Cargo.lock` 中 `pid-parse` 条目的 version 自动更新为 0.9.2。

### Task 2: 写 CHANGELOG v0.9.2 段

**Files:**
- Modify: `CHANGELOG.md`

**Step 1: 结构**

新增 `## [0.9.2] - 2026-04-21` 段，放在 `[Unreleased]` 之后、`[0.9.1]` 之前。分节：

```markdown
## [0.9.2] - 2026-04-21

### Phase 3: Provenance-aware cross-reference (Step 1–4)

把 `CrossReferenceGraph` 从"名字集合 + 小摘要"升级为"字段级 provenance
记录 + 端到端诊断"，一次覆盖四条正交视角（cluster/symbol/attribute、
relationship、object、sheet），并且为 Phase 12a 的统一规范化层做好数据
基础。所有变更纯 additive + `#[serde(default)]`，旧 consumer 字节级兼容。
```

随后列出：

1. **Step 1 — cluster/symbol/attribute provenance records**
2. **Step 2 — relationship + object provenance links**
3. **Step 3 — end-to-end provenance chain diagnostic**
4. **Step 4 — sheet-level provenance aggregation**
5. **inspect/report** 新增 5 个小节
6. **测试增长**（332 → 400）
7. **SemVer**：Patch（additive + serde default）

**Step 2: 不列的内容**

- 不列 Phase 11 / 12 计划
- 不列 writer 相关
- 不重复 v0.9.1 已经列过的 Phase 2 内容

### Task 3: 验收 + commit

**Files:**
- No further code changes expected

**Step 1: 全量**

```bash
cargo test --all-targets
cargo fmt --check
```

Expected: 全绿 + fmt 干净。

**Step 2: Commit**

```bash
git add Cargo.toml Cargo.lock CHANGELOG.md
git commit -m "docs(release): v0.9.2 — Phase 3 provenance series (Step 1–4)"
```

**Step 3: Tag（可选，仅提示用户）**

不自动打 tag。提示用户在确认 CHANGELOG 风格 OK 后执行：

```bash
git tag v0.9.2
```

## 设计约束

- 这一步不改代码行为，只写元数据
- 不自动 push、不自动 tag
- 若 fmt 失败，回到上一步修 fmt，不拆 commit

## 完成定义

- `Cargo.toml` version = `"0.9.2"`
- `CHANGELOG.md` 顶部 `[0.9.2]` 段就绪
- `cargo test` 全绿、`cargo fmt --check` 干净
- 一个 commit：`docs(release): v0.9.2 — Phase 3 provenance series (Step 1–4)`
