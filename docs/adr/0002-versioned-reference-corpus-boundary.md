# Measure completeness against a versioned reference corpus

Evidence completeness is measured against a revision-controlled corpus of PID packages, symbol definitions, reference data, and controlled fixtures. Inputs outside that corpus must fail safely and report coverage gaps, but the project does not claim automatic semantic coverage for every SmartPlant version or third-party extension; new variants enter the completeness contract only after they are registered and ratcheted.
