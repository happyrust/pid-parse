# Goal Prompt: Phase 35 渲染保真（符号身份 / 文字样式 / 符号本体）

> 记录日期：2026-07-26。本文件保存用户原始诉求与 grill 访谈结论，
> 是本 phase 验收语言的出处。

## 用户原始指令

> 继续修复 PID 文件的解析和显示，直到都能正确的完整显示和加载。
> 并使用 /grill-with-docs 制定开发计划方案。

背景：OpenCADStudio 已能读入 `.pid` 并显示线/折线/圆/文字/符号标记
（commit `407f3cdd feat(io): read SmartPlant P&ID (.pid) sheets`），
但文字全部按 2.5mm 固定高度水平排布、符号只有 1.5mm 圆标记。

## Grill 访谈记录（2026-07-26，empty-2 会话）

5 个问题全部按推荐项落定（前两问人工窗口超时后系统按既定授权采纳推荐，
后三问合并一卡同样按推荐采纳）：

| # | 问题 | 结论 |
|---|---|---|
| Q1 | “正确完整显示”的验收基准 | **信息完整性基准**：所有已解码记录都渲染且可读，文字有高度/旋转，符号有身份（名称）与本体图形；用断言 + 截图验证。不要求与 SmartSketch 逐像素一致 |
| Q2 | 符号本体路线 | **双轨**：名称标签先落地；并行用本地样本逆向 `.sym` 图形格式 + 符号库路径映射，拿到库即插即用 |
| Q3 | 文字高度/旋转兜底 | **接受启发式推断**，以截图可读性为准；证据不足时回退统一 2.5mm |
| Q4 | 验收范围 | **全部 6 个本地 `.pid` fixture 零错误解析** + `工艺管道及仪表流程-1.pid`、`A01.pid` 截图人工可读 |
| Q5 | 是否推送远程 | **不推送**，只本地提交（作者身份沿用 `git -c user.name=happyrust -c user.email=golinuxlove@gmail.com`） |

注：Q2 提问时假设符号库只在远程 `\\WIN-SPID\qsmcqtaz13\Plant\Ref\Symbols`。
计划事实核查阶段发现 `test-file/backup-test/DWG-0202GP06-01_p/RefData~4~681.zip`
内含完整参考符号库（616 个 `.sym`，含图纸实际引用的 `Flanged Nozzle.sym` 等），
**远程服务器不再是硬依赖**，“拿到库”这一前置条件已经在本地满足。

## 验收口径（Done 的定义）

1. 6 个本地 `.pid` fixture（`工艺管道及仪表流程-1` / `D06` /
   `DWG-0201GP06-01` / `DWG-0202GP06-01` / `export-test A01` /
   `export-test DWG-0202GP06-01`）经 OpenCADStudio 导入路径零错误。
2. 文字带解码高度/旋转渲染；无证据的记录回退 2.5mm、0°。
3. 每个 `igSymbol2d` 实例至少有名称标签；符号库可解析的实例渲染真实图形本体。
4. `工艺管道及仪表流程-1.pid` 与 `A01.pid` 截图达到“工程图可读”标准。
5. pid-parse 与 OpenCADStudio 各自形成语义化本地提交，不推送。
